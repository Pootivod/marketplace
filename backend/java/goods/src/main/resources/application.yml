spring:
  r2dbc:
    url: r2dbc:postgresql://${POSTGRES_ADDR}/goods
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
