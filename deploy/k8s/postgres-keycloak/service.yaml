apiVersion: v1
kind: Service
metadata:
  name: postgres-keycloak
  namespace: marketplace
spec:
  selector:
    app: postgres-keycloak
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP
