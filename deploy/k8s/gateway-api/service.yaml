apiVersion: v1
kind: Service
metadata:
  name: gateway-api
  namespace: marketplace
spec:
  selector:
    app: gateway-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
