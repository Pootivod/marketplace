apiVersion: apps/v1
kind: Deployment
metadata:
  name: favorite-api
  namespace: marketplace
  labels:
    app: favorite-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: favorite-api
  template:
    metadata:
      labels:
        app: favorite-api
    spec:
      containers:
        - name: favorite-api
          image: marketplace/favorite-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: favorite-api
  namespace: marketplace
  labels:
    app: favorite-api
spec:
  selector:
    app: favorite-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
