apiVersion: v1
kind: Service
metadata:
  name: users-api
  namespace: marketplace
spec:
  selector:
    app: users-api
  ports:
    - name: http
      port: 8082
      targetPort: 8082
  type: ClusterIP
