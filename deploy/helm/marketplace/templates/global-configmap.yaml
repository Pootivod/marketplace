{{- if .Values.global.createGlobalConfig }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: global-config
  namespace: {{ .Values.global.namespace.name }}
data:
{{- range $k, $v := .Values.global.config }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
{{- end }}
